{
  "name": "three-gpu-particle-system",
  "version": "1.0.0",
  "description": "GPU based particle system for three.js. Heavily based on tdl library (https://github.com/greggman/tdl)",
  "main": "build/three-gpu-particle-system.min.js",
  "scripts": {
    "build": "rollup -c",
    "build-uglify": "rollup -c && uglifyjs build/three-gpu-particle-system.js -cm > build/three-gpu-particle-system.min.js",
    "dev": "rollup -c -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fazeaction/three-gpu-particle-system.git"
  },
  "author": "fazeaction",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/fazeaction/three-gpu-particle-system/issues"
  },
  "homepage": "https://github.com/fazeaction/three-gpu-particle-system#readme",
  "devDependencies": {
    "rollup": "^0.34.8",
    "rollup-watch": "^2.5.0",
    "three": "^0.81.2",
    "uglify-js": "^2.6.0"
  }
}
